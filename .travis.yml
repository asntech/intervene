language: python
python:
  - "2.7"
  - "3.4"
  - "3.5"

  # PyPy versions
  #- "pypy"
  #- "pypy-5.3.1"
# command to install dependencies
install:
  - pip install -e .

# command to run tests
script:
  - intervene venn --test
  - intervene upset --test
  - intervene pairwise --test
